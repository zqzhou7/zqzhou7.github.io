---
title: ""
sitemap: false
permalink: /404.html
layout: single
author_profile: false
header:
  overlay: false
---

<style>
/* Remove theme content padding/gaps for this page */
.page__content{
  margin:0 !important;
  padding:0 !important;
}

/* =========================================
   Background hero
   Step 1: show photo clean
   Step 2: blur/dim overlay fades in
   ========================================= */
.zq-404-bg{
  min-height: 80vh;
  display:flex;
  align-items:center;
  justify-content:center;
  position:relative;
  overflow:hidden;

  background-image: url("/images/404_art.JPG");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

/* Soft edge vignette (always on, subtle) */
.zq-404-bg::after{
  content:"";
  position:absolute;
  inset:-2px;
  pointer-events:none;
  background: radial-gradient(
    ellipse at center,
    rgba(0,0,0,0.00) 35%,
    rgba(0,0,0,0.22) 100%
  );
}

/* Blur overlay (starts invisible, then appears) */
.zq-404-bg::before{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;

  backdrop-filter: blur(0px) brightness(1);
  -webkit-backdrop-filter: blur(0px) brightness(1);
  background: rgba(0,0,0,0.00);

  /* Step 2 animation: delay so the photo shows first */
  animation: zqBlurIn 1.3s ease-in-out forwards;
  animation-delay: 1.0s; /* <-- photo-only time */
  opacity: 0;
}

/* Step 2 keyframes */
@keyframes zqBlurIn{
  0%{
    opacity: 0;
    backdrop-filter: blur(0px) brightness(1);
    -webkit-backdrop-filter: blur(0px) brightness(1);
    background: rgba(0,0,0,0.00);
  }
  100%{
    opacity: 1;
    backdrop-filter: blur(8px) brightness(0.92);
    -webkit-backdrop-filter: blur(8px) brightness(0.92);
    background: rgba(0,0,0,0.14);
  }
}

/* =========================================
   Floating glass card (Step 3)
   ========================================= */
.zq-404-glass{
  position:relative;
  z-index: 2;

  padding: 42px 54px;
  border-radius: 32px;
  text-align:center;

  background: rgba(255,255,255,0.62);
  border: 1px solid rgba(0,0,0,0.10);
  box-shadow: 0 30px 90px rgba(0,0,0,0.35);

  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);

  /* Step 3 animation: start after blur begins */
  opacity: 0;
  transform: translateY(26px) scale(0.98);
  animation: zqCardIn 1.0s cubic-bezier(.2,.8,.2,1) forwards;
  animation-delay: 2.0s; /* <-- badge appears later */
}

/* Glass sheen */
.zq-404-glass::before{
  content:"";
  position:absolute;
  inset:0;
  border-radius: 32px;
  pointer-events:none;
  background: linear-gradient(
    to bottom,
    rgba(255,255,255,0.55),
    rgba(255,255,255,0.00) 45%
  );
  opacity: 0.55;
}

/* Step 3 keyframes */
@keyframes zqCardIn{
  0%{
    opacity: 0;
    transform: translateY(26px) scale(0.98);
  }
  100%{
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* Badge number */
.zq-404-code{
  font-size: clamp(64px, 12vw, 108px); /* slightly smaller */
  font-weight: 820;
  letter-spacing: -0.02em;
  line-height: 1;
  margin-bottom: 10px;
}

/* Subtitle */
.zq-404-text{
  font-size: 1.15rem;
  opacity: 0.80;
}

/* Optional: a small return link button (comment out if you don't want) */
.zq-404-home{
  display:inline-block;
  margin-top: 18px;
  padding: 10px 14px;
  border-radius: 999px;
  text-decoration: none;

  background: rgba(255,255,255,0.55);
  border: 1px solid rgba(0,0,0,0.10);
  color: rgba(0,0,0,0.82);
}
.zq-404-home:hover{ text-decoration:none; }

/* =========================================
   Dark mode
   ========================================= */
html[data-theme="dark"] .zq-404-bg::after{
  background: radial-gradient(
    ellipse at center,
    rgba(0,0,0,0.00) 30%,
    rgba(0,0,0,0.40) 100%
  );
}
html[data-theme="dark"] .zq-404-bg::before{
  animation-name: zqBlurInDark;
}
@keyframes zqBlurInDark{
  0%{
    opacity: 0;
    backdrop-filter: blur(0px) brightness(1);
    -webkit-backdrop-filter: blur(0px) brightness(1);
    background: rgba(0,0,0,0.00);
  }
  100%{
    opacity: 1;
    backdrop-filter: blur(10px) brightness(0.70);
    -webkit-backdrop-filter: blur(10px) brightness(0.70);
    background: rgba(0,0,0,0.30);
  }
}

html[data-theme="dark"] .zq-404-glass{
  background: rgba(18,18,22,0.62);
  border-color: rgba(255,255,255,0.12);
  color: rgba(255,255,255,0.92);
}
html[data-theme="dark"] .zq-404-glass::before{
  background: linear-gradient(
    to bottom,
    rgba(255,255,255,0.12),
    rgba(255,255,255,0.00) 50%
  );
  opacity: 0.55;
}
html[data-theme="dark"] .zq-404-home{
  background: rgba(255,255,255,0.06);
  border-color: rgba(255,255,255,0.12);
  color: rgba(255,255,255,0.90);
}
</style>

<div class="zq-404-bg">
  <div class="zq-404-glass">
    <div class="zq-404-code">404</div>
    <div class="zq-404-text">This page could not be sampled.</div>
    <a class="zq-404-home" href="/">Return home</a>
  </div>
</div>
